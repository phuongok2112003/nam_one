#include <iostream.h>
int  main()
{
 float  i, j, n,s; i=0 ,s=0; j=0; 
 while (n!=0) {  
 	
 
 	cout<< "so  can nhap la " ;cin>>n;s=s+n; 
	  i=i+1;
	  if (n<0)
	  j=j+1; 
	   
 
 }
 cout<<"trung binh cong  la " <<  (s/(i)); 
 cout<<"\ncac so am da nhap vao la "<<j<<" so "; 
 cout<<" \ncac so khac 0 la "<<i-1<<" so"<<endl; 
    return 0;
}